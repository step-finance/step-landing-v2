export function WalletConnectButton() {
  return (
    <button
      type="button"
      disabled
      className="inline-flex items-center justify-center rounded-full border border-line bg-white/5 px-5 py-3 text-sm text-muted"
    >
      Wallet connect arrives in a later phase
    </button>
  );
}
